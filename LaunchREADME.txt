to launch the algorithm on an instance :
launch the java class "Main" via the "run configurations" in eclipse and write the following statement in the arguments :
"instances2/d198.tsp -g -v"
this is to be adapted for the instance we want to launch